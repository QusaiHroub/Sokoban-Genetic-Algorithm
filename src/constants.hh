#ifndef CONSTATNS
#define CONSTATNS

#include <vector>

using std::vector;

#include "sokobanTypes.hh"
#include "types.hh"

struct Properties {
    const static usize geneLength = 250;
    const static usize eInsturctionsLength = 4;
    const static usize pivot = 101;
};

#endif